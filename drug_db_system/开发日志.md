docker run -d --name pg-medicine \
  -e POSTGRES_PASSWORD=postgres \
  -e POSTGRES_USER=postgres \
  -p 5433:5432 \
  -v pg-medicine-data:/var/lib/postgresql/data \
  postgres:16

docker ps
docker exec pg-medicine pg_isready -U postgres

cd /mnt/d/Desktop/CRDB/drug_db_system/backend

# 安装依赖（首次运行）
pip install -r requirements.txt

export PG_PORT=5433

# 初始化数据库（首次运行，建库 + 建表 + 导入 Excel 数据）
python setup_db.py

# 启动后端服务
python main.py

cd /mnt/d/Desktop/CRDB/drug_db_system/backend
uvicorn main:app --host 0.0.0.0 --port 8000 --reload



docker start pg-medicine

docker stop pg-medicine


优先级	优化项	预期效果
P0	建索引（商品编码、门店编码、日期、公司）	查询从分钟级→毫秒级
P0	全文搜索替代 ILIKE	搜索从全表扫描→索引查找
P1	COPY 替代逐条 INSERT	导入从小时级→分钟级
P1	Keyset 分页替代 OFFSET	翻页速度恒定，不会越翻越慢
P2	分区表	时间范围查询只扫对应分区
P2	物化视图	聚合统计报表即时返回
P3	连接池	高并发下减少连接开销


drug_db_system/
├── backend/
│   ├── app/                          ← 应用主包
│   │   ├── main.py                   ← FastAPI 入口（创建 app、注册路由）
│   │   ├── config.py                 ← 配置（DB 参数、路径）
│   │   ├── database.py               ← 连接池 + get_conn
│   │   ├── schemas.py                ← Pydantic 模型
│   │   ├── dependencies.py           ← 公共依赖（db_key 校验）
│   │   ├── models/schema_def.py      ← 表结构 + 索引/FTS 元数据
│   │   ├── routers/                  ← 路由层（新增功能加这里）
│   │   │   ├── meta.py               ←   /api/dbs
│   │   │   ├── rows.py               ←   /api/{db}/rows CRUD
│   │   │   ├── export.py             ←   /api/{db}/export + /export/stream
│   │   │   └── stats.py              ←   /api/{db}/stats 统计
│   │   ├── services/                 ← 业务逻辑层
│   │   │   ├── query_builder.py      ←   查询构建（全文搜索/游标分页）
│   │   │   └── data_import.py        ←   COPY 导入 + 索引创建
│   │   └── utils/serialization.py    ← JSON 序列化
│   ├── main.py / setup_db.py / create_indexes.py  ← 顶层脚本
│   └── requirements.txt
├── frontend/index.html               ← 前端（已修复+增强）
└── sql/schema.sql, indexes.sql       ← SQL 参考


目前node miniforge都在wsl中

因为门店数的难点是非重复计数 每个维度的变化都会影响最终的门店数数量

# 数据预处理导入前处理
高济去掉企业编码把销售数量改成数值，新增月度列

大参林高济日期都要转成文本型数字

海王数据在点击详细数据后销量类似存在文本，需要转换成数值型否则求和不一致 新增月度列，删掉最后一行合计
海王数据目前只录入了广州分部和深圳分部
销售数量都要转数字型
日期都要转文本型数字

上周的原始数据都拉错了月份没拉对总日期原始数据少了，下次拉数据要核对行数和总数