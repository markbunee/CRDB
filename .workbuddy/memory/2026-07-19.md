# 2026-07-19 工作日志

## 医药销售数据库管理系统搭建
- 用户需求：根据三份 Excel（大参林/高济/海王）表头建 3 个 PostgreSQL 库，Python 后端 + 简单前端，并说明如何起 PostgreSQL 服务。
- 读取三个 xlsx 表头（用 pandas，因 Windows 控制台 GBK 乱码，改写到 UTF-8 JSON 文件再读取）：
  - 大参林：Sheet1，4 行 × 23 列（日期/月度/公司/来源公司/门店编码...销售价格/生产厂家）
  - 高济：Sheet1，7 行 × 14 列（业务日期/企业名称/门店编码[含字母]/商品名称/销售数量/供应商名称）
  - 海王：Sheet1，6 行 × 13 列（类别/商品SAP编码/店号·区域ID[混合]/销量/合同价/事业部）
- 交付项目：`D:\ASUS\CRDB\drug_db_system\`
  - `backend/schema_def.py` 三个库的字段定义（中文列名作 PostgreSQL 引号标识符）
  - `backend/setup_db.py` 一键建库建表+导入 Excel
  - `backend/main.py` FastAPI 后端（增删改查/搜索/排序/导出 CSV）+ 服务前端
  - `frontend/index.html` 单页前端（Tab 切换三库、分页、搜索、排序、增删改、导出）
  - `sql/schema.sql` 建表 SQL 参考
  - `启动PostgreSQL_Docker.bat` / `一键启动.bat` / `仅启动Web.bat` / `如何启动PostgreSQL服务.md` / `README.md`
- **关键发现**：本机 5432 端口已有原生 PostgreSQL 运行（密码非 postgres，报错 GBK 本地化）。测试时 Docker 容器改用 5433 端口避开冲突，端到端验证全部通过（建库/建表/导入 4-7-6 行/增删改查/中文搜索/中文列名排序/导出）。
- Windows 编码坑：psycopg2 在中文系统会把 libpq 错误信息本地化成 GBK，导致 utf-8 解码失败。已在 config.py 设 `PGCLIENTENCODING=UTF8`、bat 设 `PYTHONUTF8=1` 修复（密码正确时无错误不触发）。
- Python 环境：`C:\Users\ASUS\.workbuddy\binaries\python\envs\default`（已装 pandas/openpyxl/psycopg2-binary/fastapi/uvicorn）。
